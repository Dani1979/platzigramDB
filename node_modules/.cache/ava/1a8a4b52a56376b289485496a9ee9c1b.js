'use strict';

const test = require('ava');

const utils = require('../lib/utils');

test('Extracting hashtags from text', t => {
  let tags = utils.extractTags('a #picture with tagas #AwEsOmE #Platzi #AVA and #100 ##yes');
  t.deepEqual(tags, ['picture', 'awesome', 'platzi', 'ava', '100', 'yes']);

  tags = utils.extractTags('a picturo with no tags');
  t.deepEqual(tags, []);

  tags = utils.extractTags();
  t.deepEqual(tags, []);

  tags = utils.extractTags(null);
  t.deepEqual(tags, []);
});
//# sourceMappingURL=../node_modules/.cache/ava/1a8a4b52a56376b289485496a9ee9c1b.js.map